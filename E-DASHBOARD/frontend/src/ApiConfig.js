const url = "http://127.0.0.1:4000";
const ApiConfig = {
    register: `${url}/register`,
    login: `${url}/login`,
    logout: `${url}/logout`,
    profile: `${url}/profile`,
    update: `${url}/update`,
    delete: `${url}/delete`,  
  };
  export default ApiConfig;